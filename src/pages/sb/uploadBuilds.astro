---
import Layout from "@/layouts/Layout";

import { supabaseService } from "@/library/supabase";

const res = await supabaseService.from("images").upload(uploadPath, file, {
	metadata: {
		custom: "metadata",
		second: "second",
		third: "third",
	},
});
---

<Layout />
