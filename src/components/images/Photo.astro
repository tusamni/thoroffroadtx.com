---
// components
import { Picture } from "@astrojs/image/components";

// functions
import { getImage } from "@/functions/Media";

// props
export interface Props {
    src: string;
    widths?: Array<string>;
    sizes?: string;
}
const { src, widths = [400, 800, 1600, 2000], sizes = "100vw", ...attrs } = Astro.props as Props;

const image = await getImage(src);
---

<Picture src={image.src} alt={image.alt} formats={["jpeg", "webp"]} aspectRatio={image.aspectRatio} widths={widths} {...attrs} />
