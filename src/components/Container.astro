---
interface Props {
    classes?: string;
    padding?: boolean;
}
const { classes, padding = true } = Astro.props;
---

<div class:list={[`mx-auto max-w-screen-xl sm:px-6 md:px-8`, padding && `px-4`, classes]}>
    <slot />
</div>
