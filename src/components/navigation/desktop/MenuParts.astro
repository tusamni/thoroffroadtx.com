---
// components
import { getCollection } from "astro:content";
import { Icon } from "astro-icon";

// content
const allParts = await getCollection("parts");
---

<div class="relative" x-data={`{ partsMega : false }`} x-on:mouseenter={`partsMega = true`} x-on:mouseover.away={`partsMega = false`} x-on:click={`partsMega = true`} x-cloak>
    <a href="/parts" class="inline-flex items-center gap-x-2 first:font-semibold leading-6 text-white transition px-4 py-3" x-bind:class={`partsMega ? 'bg-red-500' : ''`}>
        <span>Parts</span>

        <Icon name="Chevron" class="w-4 h-4 transition" x-bind:class={`partsMega ? 'rotate-180' : ''`} />
    </a>

    <div
        x-cloak
        x-transition:enter="transition ease-out duration-300"
        x-transition:enter-start="opacity-0"
        x-transition:enter-end="opacity-100 scale-100"
        x-transition:leave="transition ease-in duration-100"
        x-transition:leave-start="opacity-100 scale-100"
        x-transition:leave-end="opacity-0"
        x-show="partsMega"
        class="w-[40em] absolute top-0 mt-12 z-10 bg-gradient-to-r from-gray-100 via-gray-200 to-gray-300 shadow-lg ring-1 ring-gray-900/5">
        <div class="grid grid-cols-2">
            {
                allParts.map((p) => (
                    <a href={`/parts/${p.slug}`} title={p.data.title} class="flex flex-col gap-x-4 text-sm font-semibold leading-6 text-gray-900 hover:bg-white px-6 py-4 transition">
                        {p.data.title}

                        <ul class="comma-list line-clamp-1 text-xs font-light text-stone-500">
                            {p.data.brands.map((b) => (
                                <li>{b.title}</li>
                            ))}
                        </ul>
                    </a>
                ))
            }
        </div>
    </div>
</div>
