---
interface Props {
    theme?: string;
    size?: string;
}
const { theme = "primary", size = "base", ...attrs } = Astro.props;

// pill theming
const base = "font-semibold w-fit px-3 py-2 rounded-sm";
const themes = {
    primary: ["text-stone-700 bg-white"],
    secondary: ["text-stone-100 bg-stone-500"],
};
---

<div class:list={[base, themes[theme], `text-${size}`]} {...attrs}>
    <slot />
</div>
