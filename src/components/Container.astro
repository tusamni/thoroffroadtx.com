---
interface Props {
    classes?: string;
    padding?: boolean;
}
const { classes, padding = true } = Astro.props;
---

<div class:list={[`mx-auto max-w-screen-xl`, padding && `px-6 md:px-8`, classes]}>
    <slot />
</div>
