---
// props
interface Props {
    url: string;
    classes: string;
}
const { url, classes } = Astro.props;
---

<a href={url} class:list={["group bg-gray-700/30 grid md:grid-cols-2 lg:grid-cols-1 xl:grid-cols-2 gap-6 md:gap-12 rounded-md sm:p-6 md:p-12", classes]}>
    <slot />
</a>
