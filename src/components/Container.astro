---
interface Props {
    classes?: string;
    padding?: boolean;
}
const { classes, padding = true } = Astro.props;
---

<div class:list={[`mx-auto max-w-screen-xl md:px-8`, padding && `px-6`, classes]}>
    <slot />
</div>
