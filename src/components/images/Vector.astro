---
export interface Props {
    src: string;
    classes: string;
}

const { src, classes } = Astro.props as Props;
const { default: innerHTML } = await import(src);
---

<div set:html={innerHTML} class={classes}></div>
